import json
from pathlib import Path
root = Path(__file__).resolve().parents[1]
cfg = json.loads((root / 'reseller-defaults.json').read_text())
(root / 'backend/internal/pkg/reseller/defaults_gen.go').write_text('// Code generated by tools/generate_reseller_defaults.py; DO NOT EDIT.\npackage reseller\n\nconst DefaultMultiplier = ' + repr(cfg['default_multiplier']) + '\nconst MinimumMultiplier = ' + repr(cfg['minimum_multiplier']) + '\nconst PageSize = ' + repr(cfg['page_size']) + '\n')

# GoToCC 0.2.4+custom.008

在“用户管理 → 编辑用户”新增“邀请码与返佣归属”面板。永久邀请码与 AFF 邀请码使用不同标签和颜色；显示客户当前关联的码及推广人，输入新码后解析用户名、邮箱和 ID，随底部“更新”一起保存。

- 永久邀请码按该码绑定的归属用户解析，AFF 按码所属用户解析；已有客户可以补绑或转到其他推广人名下，不消耗注册码次数、不修改码本身的所有者。
- 客户基础资料、邀请码关联、直接邀请人、双方直属客户数和变更记录在同一事务提交；保存时重新核对归属版本和码的所属用户，不允许自指或成环。
- 原始注册邀请码和永久码使用记录保留，历史佣金不转移。新充值按最近三代返佣规则处理；换绑也可能改变下级后续充值在最近三代内经过的受益人。
- 在线有效充值在实际入账事务固定受益链，延迟处理和重试沿用该链；管理员充值沿用同事务返佣。比例、开关、冻结期限仍按既有结算时规则读取，不增加兑换、赠额或套餐返佣。

新增 migration `271_affiliate_inviter_reassignment.sql`：增加当前邀请码关联、关系版本和生效时间，新增归属变更记录及支付受益链快照。历史 SQL 不改；存量已入账但未返佣的订单按升级时关系初始化并标记 `legacy_upgrade`，不声称还原历史入账时关系，终态订单不重新返佣。

迁移会更新已绑定用户的当前关联字段，写入必要的存量订单快照，持有表锁并产生 WAL，磁盘随用户和待结算订单数量增长。本地迁移前保留备份，新旧核心不重叠。换绑或产生新快照后，旧程序无法保证待结算订单的归属，不能仅回退二进制；保留数据并前向修复。

本版包含 `.007` 的上游升级：Plus `v0.2.4+custom.004`（`81505b0c2e35ef9056d55f357a5c4e29c24c2ac9`）、统一出站身份、真实推理周额度重置证据和自有 migration 270。此前回滚限制继续适用。交付资源和线上配置格式不变。

本地最终包运行后交用户人工验收；没有自动发布或更新生产。

# GoToCC 0.2.4+custom.006

邀请返利页的统计卡片、三代分佣说明和最近三代邀请名单，只向在“永久邀请码”中被绑定为归属用户的账号展示。普通用户继续保留邀请入口、邀请码、分享链接和既有返利转余额区。

- 页面每次读取时按现存归属绑定决定展示；不要求永久码仍可注册使用，不新增管理员个人页特例。
- GET /api/v1/user/aff 新增 show_rebate_details 布尔字段，现有响应数据与接口行为保留。本次只控制界面展示，不改变佣金获得资格、已有邀请关系、充值返佣或转余额规则。
- 注册时，手填永久邀请码若有归属用户，先绑定该归属；已有直接邀请人不会被链接里的 AFF 覆盖。该规则沿用现有版本，没有修改。
- 继承 `.005` 邀请列表 SQL 修复、三代返佣与 PR #6 智能路由优先级。上游仍为 Plus `v0.2.4+custom.002`（`fdb9c6de8a959056d6678778979b60c0b0bf20e6`），官方基线为 Sub2API `v0.2.4`。
- 相对 `.005` 无新增 migration、配置、数据回填或包外运行资源。换回 `.005` 会恢复所有用户可见这三部分；此前迁移与资金写入的回滚限制仍然适用，见 `docs/GOTOCC_PLUS_MIGRATION.md`。

本版按本地最终包运行和用户人工验收流程交付，不新增或运行全量测试；没有自动更新生产。

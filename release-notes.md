GoToCC 0.2.1+custom.005

## 视频请求与计费修复

统一 `/v1/videos` 接受 JSON 和 multipart。路由、审核、计费读取独立参数视图；上游收到原请求正文、Content-Type 和参考文件，只有显式模型映射修改 model。模型名称不决定请求编码或字段转换。

创建任务按明确的请求秒数和价格档位冻结费用，移除统一视频路径的 15 秒限制和未知分辨率回落 480p。支持表单字符串秒数以及 JSON 整数；按次计费使用视频数量，按秒计费使用完整请求秒数。仅使用已配置价格，未配置档位明确返回错误，不猜测低档价格。保留原账号轮询、content 可读后结算、失败释放和账务幂等。

上游基线仍为 Plus v0.2.1+custom.002（1b95c72f186275f582714bed38a1a4af122429f1）。

## 升级与回滚

从自有 0.2.1+custom.004 升级没有新增 SQL migration、schema、配置字段或 Redis 迁移；不修改历史任务、账单或余额。新任务冻结原有结构支持的秒数、档位和金额。回退旧程序会恢复 JSON-only 创建、15 秒限制及旧价格取值行为，不能让依赖 multipart 的新 Canvas 继续提交；回滚需先停止新的提交并配套回退 Canvas。保留当前数据库，不恢复旧 dump 覆盖自然写入。

## 本地验收范围

使用本地模拟上游验证请求文件和字段、显式模型映射、按次与按秒费用、缺失档位及终态账务。最终 Linux/amd64 发行包用于本地人工验收，通过后只发布同一包。未创建付费生成，供应商实际生成由用户验证。

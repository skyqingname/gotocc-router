# GoToCC 0.2.7+custom.005

基于已发布的 GoToCC 0.2.7+custom.003 与本地 custom.004 候选修复。Plus 基线仍为 v0.2.7+custom.001（7b2d38cc501b0e73302c2609ee79f39f75d3c5e8），未引入其他上游升级。

- AFF 与永久邀请码统一：个人默认码和管理员邀请码在同一目录管理，同一码用于注册准入与返佣归属。旧码原值、关系、历史账目保留。注册与管理员换绑只输入一个邀请码，旧分享链接与旧请求字段继续作为同一码的别名使用。
- 代理中心：新用户点击申请立即成为代理，无表单、无人工审核、不可退出。后台改为只读代理名单；三代资格逐代判断，申请前不计佣、不补发。存量用户自动保留资格。
- 修复代理接口处理器缺失造成的 500、查询失败被误认作未申请，以及代理身份和返佣界面可见性不一致。
- 站长中心仅展示客户消费差价，移除邀请佣金统计与专属返佣比例；同一用户的代理佣金在代理中心查看。

## 数据变化与回滚

从已发布 custom.003 更新会执行 279 与 280：279 建立代理表，首次启动记录 cutoff 并回填存量；280 将既有 AFF 原码补入统一邀请码目录，并把旧候选中已提交的待审核/被驳回申请转换为已开通，生效前不补发。历史 migration、资金流水、邀请关系与 Redis 格式不变。邀请码生成参数和初始化超时在根 affiliate-defaults.json 中声明。

新增目录记录及索引产生与用户数相应的存储和 WAL；代理初始化需原子事务及短时锁，失败会停止启动。升级前备份数据库并保持单 writer。已发生目录限制、申请和注册写入后，旧二进制无法维持同一行为；应保留数据向前修复，不能仅回退二进制或用旧 dump 覆盖当前数据。

本地最终包交由用户人工验收；发布复用同一包。GitHub 发布不等于生产更新，生产仍由用户通过自有网页更新。

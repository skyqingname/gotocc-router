# GoToCC 0.2.5+custom.003

保留 `0.2.5+custom.002` 的全部功能，合入 PR #11，恢复正数余额兑换码的三代邀请返佣。002 已为不可变 Release，本次使用新版本号交付修复。

上游基线：Plus `v0.2.5+custom.001`，commit `ddf18b386974067fa794cc1822238ba0cda37e22`；官方 Sub2API `v0.2.5`。

## 本次修复

- 用户手动兑换与管理员 `create-and-redeem` 自动兑换使用相同返佣规则：以实际入账 USD 代币为基数，沿入账时最近三代邀请关系计算。
- 兑换码消费、余额增加和佣金流水在同一事务内提交；重复兑换不会再次入账或返佣。
- 普通余额码受返佣总开关控制，不受管理员余额充值返佣开关控制；保留全局比例、站长专属比例和冻结期。
- 关联平台在线支付订单的兑换码继续由订单完成入口返佣一次。负数余额、并发和订阅兑换不参与余额返佣。
- 后台流水增加“兑换码充值”来源名称，同步中英文接入文档。
- 本次不新增 migration、配置、运行时依赖或历史回填。升级前创建但未使用的正数余额码，在修复后兑换也参与返佣；已使用码不会自动补发历史佣金。

## 保留的 002 功能

- 普通与订阅分组的多时段倍率；请求和 WebSocket 每轮冻结实际倍率。
- Video 分组内按模型配置协议及参数，包含固定影策来源的 39 个视频 provider、声明式请求/查询/结果规则、Google/TC3/火山鉴权及任务协议快照。独立 Video 账号支持 API Key、供应商地址和脱敏 Secret Key 编辑。
- 视频任务返回稳定本地 ID，保留旧上游 ID 查询，支持 singular video/generations 及无 v1 别名。
- 受邀站长中心、客户定价、消费差价展示及站长三代返佣比例；TypeSafe Jev 审核；邀请码、团队、智能路由、异步图片和自有更新通道。

## 升级与回滚

从 002 升级不新增数据库迁移。更早版本仍会执行 002 已有迁移 272–278：OpenCode 平台约束、清理全 NULL 的历史配额行、代理时区/国家元数据、分组多时段配置、视频任务 provider_config、站长配置/客户/价格/收益及任务快照、分组 video_models 与原渠道配置复制。历史 SQL 保持不变。

较早版本升级涉及短时 DDL 锁、配额扫描与删除、分组 JSON 回填及 WAL 增量，需按现有网页更新流程备份并预留空间。旧程序不理解新时段、视频协议与站长账务；产生新写入后保留当前数据向前修复，不能只换旧程序或用旧 dump 覆盖当前数据。回退到 002 会重新出现普通兑换码不返佣，已入账佣金不撤销。

发行包含 Linux/amd64 压缩包、更新器要求的 checksums.txt、定价 JSON 与定价清单。发布后由用户在自有版本面板更新并按提示重启；GitHub 发布不代表生产部署，历史漏返佣需另行按核账清单处理。本次未逐厂商发起付费视频生成。

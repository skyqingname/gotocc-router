# GoToCC 0.2.7+custom.004

上游基线：Plus `v0.2.7+custom.001`，commit `7b2d38cc501b0e73302c2609ee79f39f75d3c5e8`；官方 Sub2API `v0.2.7`，commit `aea725f2ea644d5592d0bbb1d63b607efa7e200a`。本版基于已发布的 GoToCC `0.2.7+custom.003`，保留其功能与修复，含 GPT-6 Sol/Luna 与 Claude Opus 5.5 官方默认价格。

## 新增：代理中心（LC-024）

代理中心是新的用户自助入口，与既有的站长中心并存但职能分开：代理中心只做三代邀请返佣，入口对所有用户可见；站长中心只做客户消费差价，入口对用户不可见，仍由管理员按需直接开放。

- 用户在返利页看到「申请成为代理」按钮，点击即提交，不需要填写任何内容。
- 提交后显示审核中；管理员在「邀请返利 → 代理审核」中看到申请人、注册时间和申请时间，选择通过或驳回。
- 审核通过后才出现返利统计、最近三代名单和「返利额度转余额」。未申请或审核未通过的用户看不到这些界面，也不产生佣金。
- 被驳回的用户可以重新申请；重新申请后状态回到审核中。
- 审核通过后不提供退出、撤销或降级入口，用户端与管理端均无此操作。

## 变更：站长专属返佣比例移除

`0.2.7+custom.003` 及更早版本允许管理员为每位站长单独设置一、二、三代返佣比例。本版删除该设置：站长作为返佣受益人时与其他用户适用同一全局比例，并同样需要持有已通过的代理身份。站长设置弹窗只保留「开放站长中心」开关。

站长中心的客户定价与消费差价收益不受影响，`rebate_rates` 列保留在数据库中但不再读写，已有取值不清零。

## 变更：存量用户保持既有返佣

本次升级按注册时间区分存量与新增用户，边界为**新程序首次启动服务的时刻**，不是构建时间或迁移执行时间。

- 边界之前的全部在册用户由升级流程一次性登记为已通过的代理（来源标记为存量自动通过），此后永远有效，不需要提交申请，也不因边界后续变动而失去资格。
- 边界之后注册的用户必须经审核通过才产生返佣。三代链条上每一代独立判定：某一代没有已通过的代理身份时该代不返，不补发给其他层级，也不影响其余代的发放。
- 邀请码注册、注册绑定、客户归属和返利明细的既有关系一律不变；门槛只作用于「能否获得返佣」。

## 数据库与回滚

本版新增一个 SQL migration（`279_agent_profiles.sql`），只创建 `agent_profiles` 表及其索引，不做数据回填；边界写入与存量登记由新程序首次启动时在同一事务中完成，并带原子条件，多个实例并发启动也不会重复或移动边界。

- 升级到本版后，`agent_profiles` 已按边界一次性写入存量用户，属于前向业务写入。回退到旧二进制后，代理审核状态与返佣门槛不再生效，但已产生的佣金记录保留；不能用旧 dump 覆盖当前数据。
- 旧版没有该表，回退时新增表闲置，不影响旧程序运行。
- 相对上一版没有配置项、Redis 数据格式或历史资金回填变化。

## 升级与回滚

发行包包含 Linux/amd64 压缩包、更新器要求的清单、定价 JSON 和定价清单。二进制与定价资源组成同一发行集合；回退时保持集合匹配。GitHub 发布不代表生产已更新；用户从自有版本面板更新并按提示重启。

# GoToCC 0.2.4+custom.005

修复管理员“邀请记录”页面显示 `internal error` 和空列表的问题。

统一永久邀请码后，列表输出与 GROUP BY 使用了不同的邀请码表达式，导致 PostgreSQL 拒绝查询。本版按实际使用的两个邀请码字段分组，恢复邀请记录读取，并保留累计返利与邀请码排序。

- 基于已发布 GoToCC `0.2.4+custom.004`；上游仍为 Sub2API Plus `v0.2.4+custom.002`（`fdb9c6de8a959056d6678778979b60c0b0bf20e6`），官方基线为 Sub2API `v0.2.4`。
- 保留三代返佣、永久邀请码归属和 PR #6 智能路由优先级展示。本次仅修复邀请列表查询，不更改邀请关系、余额或返佣计算。
- 相对 `.004` 无新增 migration、配置或包外运行文件；不需要恢复备份、重建邀请记录或重新绑定用户。
- 从更早版本升级时，仍按原顺序执行已有迁移，兼容与回滚限制见 `docs/GOTOCC_PLUS_MIGRATION.md`。迁移或资金写入不能通过替换旧程序撤销；回到 `.004` 会重新出现本次列表错误。

本地原 SQL 已复现错误，修正 SQL 可正常执行。发行采用本地最终包运行及人工验收，不新增或运行全量测试。完整包发布后，用户在网页自有版本入口更新并按提示重启。

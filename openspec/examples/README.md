# OpenSpec examples

The directories here are tracked templates, not active changes. Copy the demo
into `openspec/changes/` under a descriptive change name, replace its content,
and keep the resulting plan local. Do not include `openspec/changes/` in a
commit or pull request.

Durable behavior belongs in the owning documentation and automated tests.

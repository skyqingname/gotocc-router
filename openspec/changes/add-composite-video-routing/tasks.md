# Tasks

- [x] Route OpenAI `/videos/generations` creation, status, and content aliases to the OpenAI-compatible video handler.
- [x] Normalize legacy aliases to the canonical NewAPI `/v1/videos` task surface.
- [x] Keep native Grok edits/extensions and the Composite route contract unchanged.
- [x] Add backend route/URL tests and a frontend no-Composite regression test.
- [x] Align channel request validation with the existing video billing mode and cover it with a handler regression test.
- [x] Document LC-012 and the separately authorized production group audit.
- [x] Pass focused tests and the repository release gate.

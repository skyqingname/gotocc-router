interface:
  display_name: "Sub2API Push CLI"
  short_description: "Prefer mandatory Apple Containers on macOS, then push"
  default_prompt: "Use $push-cli to run strict local checks, require installed Apple Containers on macOS before any Docker fallback, require Docker inside running WSL2 Linux on Windows without host fallback, and push the current branch safely."

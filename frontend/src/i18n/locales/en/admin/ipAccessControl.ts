export default {
  ipAccessControl: {
    title: 'IP Access Control',
    description: 'Manage global IP access rules and automatic blocks for local login failures.',
    backToSettings: 'Back to Settings',
    trustedProxy: {
      title: 'Trusted Proxy Status',
      description: 'Read-only view of the trusted proxies loaded at startup and the IP resolution result for this request. A detected forwarding header is not automatically trusted.',
      clientIp: 'Resolved Client IP',
      directPeerIp: 'Direct Peer IP',
      directPeerTrust: 'Direct Peer Trusted',
      forwardedHeaders: 'Detected Forwarding Headers',
	      identitySafety: 'Enforcement Identity',
	      safe: 'Safe for global enforcement and automatic counting',
	      unsafe: 'The proxy chain or client identity is incomplete; enforcement will reject the request',
	      legacyMode: 'Legacy Forwarded-Header Compatibility',
	      enabled: 'Enabled; used only by legacy API-key ACL/session compatibility, not global IP enforcement',
	      disabled: 'Disabled',
      emergencyAllowlist: 'Emergency Recovery Allowlist',
      emergencyConfigured: '{count} deployment recovery rule(s) configured',
      globalEnforcement: 'Global Enforcement Readiness',
      manualBlocking: 'Manual Block Creation Readiness',
      ready: 'The current request meets the safety requirements',
      notReady: 'The current request does not meet the safety requirements',
      loadedCidrs: 'Loaded Trusted Proxy IPs / CIDRs',
      none: 'None',
      noneConfigured: 'No trusted proxy addresses are loaded.',
      guideHelp: 'This page only shows the trusted proxies loaded at startup. Change the deployment file and restart the process. Saving settings in the web UI does not load this list.',
      guideLink: 'View setup steps',
      states: {
        configured: 'Configured',
        not_configured: 'Not Configured',
        empty: 'Explicitly Disabled',
        invalid: 'Invalid Configuration'
      },
      messages: {
        notConfigured: 'SERVER_TRUSTED_PROXIES is not configured. Global enforcement and manual blocks cannot be enabled until you explicitly configure trusted proxies, or explicitly use an empty list for a direct deployment.',
        empty: 'SERVER_TRUSTED_PROXIES is explicitly empty, so forwarded client IP trust is disabled. This request uses only the direct peer IP and is eligible for enforcement when no forwarding headers are present.',
        invalid: 'The trusted proxy list contains an invalid IP or CIDR, so the full trusted proxy chain is disabled at startup. Correct the deployment configuration and recreate the app container.',
        applied: 'The direct peer for this request is trusted, and the client IP was resolved through the trusted proxy chain.',
        trustedPeerNoForwarded: 'The direct peer is trusted, but no different client IP was resolved. Verify that the reverse proxy sends X-Forwarded-For or X-Real-IP.',
        notApplied: 'Trusted proxies are configured, but this request’s direct peer is outside those ranges, so the request is treated as a direct connection.'
      }
    },
    protection: {
      title: 'Automatic Protection',
      description: 'Disabled by default. When enabled, block rules apply to every application entry point, including readiness.',
      enforcement: 'Enable Global IP Enforcement',
      enforcementHint: 'Enforce manual blocks and automatic blocks globally. Only the container health endpoint remains exempt. The system-settings master switch must also be on; this toggle alone does not block traffic.',
      masterSwitchOff: 'The system-settings master switch for Global IP Access Control is off, so no requests are being blocked.',
      autoBlock: 'Automatically Block Login Failures',
      autoBlockHint: 'Counts credential-level password, login 2FA, and Passkey failures by source IP across accounts. Successful authentication does not clear the IP window.',
      threshold: 'Failure Threshold Within Window',
      window: 'Counting Window (minutes)',
      duration: 'Block Duration (minutes)',
      proxyHint: 'Explicitly configure SERVER_TRUSTED_PROXIES before enabling global enforcement: use direct proxy CIDRs for Cloudflare/reverse-proxy deployments, or an explicit empty value only for a direct deployment. Direct requests cannot supply a trusted CF-Connecting-IP, X-Forwarded-For, or X-Real-IP value.',
      proxyNotReady: 'The current proxy chain is not eligible for global enforcement. Explicitly configure trusted proxies and correct forwarding headers first; allow rules, releases, and counter resets remain available for recovery.'
    },
    failureStates: {
      title: 'Login Failure States',
      description: 'Shows every unexpired source-IP failure window, including sources below the threshold. Successful authentication does not clear this IP-only counter.',
      refresh: 'Refresh',
      lastUpdated: 'Updated {time}',
      unavailable: 'Status unavailable; displayed data may be stale',
      searchPlaceholder: 'Search IP address',
      ip: 'IP Address',
      failureCount: 'Failures in Window',
      windowStartedAt: 'Window Started',
      lastFailedAt: 'Last Failure',
      windowExpiresAt: 'Window Expires',
      blockStatus: 'Current Block Status',
      blocked: 'Blocked',
      observing: 'Observing {count}/{threshold}',
      ruleNotEnforced: 'Rule active; enforcement off',
      suppressedByAllow: 'IP covered by an allow rule',
      emergencyAllowlisted: 'IP covered by the emergency allowlist',
      ruleActive: 'Active block rule',
      resetCounter: 'Reset Counter',
      manualBlock: 'Manual Block',
      blocking: 'Blocking…',
      viewRule: 'View Rule',
      handleAllow: 'Review Allow Rules',
      emergencyAllowActive: 'Emergency Allow Active',
      manualBlockTitle: 'Manually Block Login-Failure Source IP',
      manualBlockMessage: 'Permanently block IP {ip}? This blocks only the IP, never the account, and does not clear the existing failure counter.',
      manualBlockSuccess: 'The IP was permanently blocked and applied to the current runtime policy',
      alreadyBlocked: 'The IP was already effectively blocked; its permanent manual rule is confirmed in the current runtime policy',
      manualBlockDisabledStale: 'Status data is unavailable or may be stale; refresh it first',
      manualBlockDisabledEmergencyAllow: 'The deployment emergency allowlist covers this IP; change deployment configuration before blocking it',
      manualBlockDisabledAllow: 'An active allow rule covers this IP; review or remove that rule first',
      manualBlockDisabledAlreadyBlocked: 'This IP already has an active or effective block',
      manualBlockDisabledEnforcement: 'Global IP enforcement is off; enable the master switch and page-level enforcement first',
      manualBlockDisabledIdentity: 'The current proxy chain cannot safely identify client IPs; correct the trusted-proxy configuration first',
      manualBlockDisabledBusy: 'Failure status is refreshing or another block action is being submitted',
      operationHint: 'Resetting a counter does not release a block. Manual blocks from this table are permanent and do not clear the failure counter or block an account.',
      resetTitle: 'Reset Login Failure Counter',
      resetMessage: 'Clear the login-failure counter for IP {ip}? This only clears the counter and does not release any active block rule.',
      resetSuccess: 'Login failure counter reset',
      empty: 'No matching login failure state exists in the current counting window'
    },
    rules: {
      title: 'Rules',
      description: 'Manage manual blocks, automatic blocks, and operational allow rules. Allow rules take precedence and should be used only for explicit operator recovery.',
      add: 'Add Rule',
      addTitle: 'Add IP Rule',
      searchPlaceholder: 'Search IP, CIDR, or reason',
      ip: 'IP / CIDR',
      type: 'Rule Type',
      reason: 'Reason',
      expiresAt: 'Expires At',
      never: 'Never',
      failureCount: 'Failures at Block Trigger',
      hitCount: 'Recorded Block Events',
      lastSeenAt: 'Last Recorded Block',
      lastSeenNever: 'Not recorded yet',
      hitCountHint: 'To avoid attacker-amplified database writes, block events from the same source are rate-limited and aggregated.',
      manualBlock: 'Manual Block',
      manualBlockUnavailable: 'The current proxy chain cannot safely identify the client IP, so manual blocking is disabled. You may still create an operational allow rule or correct the deployment configuration first.',
      autoBlock: 'Automatic Block',
      allow: 'Operational Allow Rule',
      statusActive: 'Active',
      statusReleased: 'Released',
      statusRemoved: 'Removed',
      statusExpired: 'Expired',
      releaseReset: 'Release and Reset',
      removeAllow: 'Remove Allow Rule',
      resetCounter: 'Reset Counter',
      releaseTitle: 'Release IP Block',
      releaseMessage: 'This releases the rule and also clears the IP’s login-failure counter.',
      removeAllowTitle: 'Remove Operational Allow Rule',
      removeAllowMessage: 'This only removes the operational allow rule. It does not clear the source’s existing login-failure counter.',
      empty: 'No matching IP access rules'
    },
    deployGuide: {
      title: 'Configure trusted proxies',
      intro: 'Before enabling global enforcement or creating a manual block, you must explicitly configure trusted proxies. List only the hop that connects directly to this process. A detected forwarding header is not automatically trusted.',
      whenNeeded: 'Wait until Trusted Proxy Status on this page is healthy. Then turn on the system-settings master switch, and only then enable enforcement here.',
      pickDeploy: 'Choose your deployment method and follow only those steps.',
      docker: 'Docker Compose',
      binary: 'Official Linux binary',
      standalone: 'Self-built / foreground process',
      binaryTitle: 'Official Linux binary (install.sh + systemd)',
      binaryWhere: 'Set server.trusted_proxies in /opt/sub2api/config.yaml. That is the file written by install.sh and the setup wizard.',
      binaryStep1: 'Confirm the service exists: systemctl status sub2api',
      binaryStep2: 'Look for the key: sudo grep -n trusted_proxies /opt/sub2api/config.yaml',
      binaryStep3: 'If a local Nginx/Caddy proxies to 127.0.0.1, add trusted_proxies under the existing server: block and list 127.0.0.1/32. Do not create a second top-level server key.',
      binaryStep4: 'If the process is exposed directly with no reverse proxy, set trusted_proxies: []. The key must stay present; deleting it leaves the policy unconfigured and enforcement stays locked.',
      binaryYamlHint: 'Local-proxy example. 203.0.113.10/32 is a documentation address. Replace the emergency allowlist with your fixed public egress IP, or use [].',
      binaryYaml: 'server:\n  trusted_proxies:\n    - 127.0.0.1/32\n  ip_access_emergency_allowlist:\n    - 203.0.113.10/32',
      binaryStep5: 'sudo chown sub2api:sub2api /opt/sub2api/config.yaml && sudo chmod 600 /opt/sub2api/config.yaml',
      binaryRestart: 'sudo systemctl restart sub2api',
      binaryStatus: 'sudo systemctl status sub2api',
      binaryCustomFile: 'If the systemd unit sets CONFIG_FILE or DATA_DIR, edit that file instead of /opt/sub2api/config.yaml.',
      binaryEnvOverride: 'If the unit already sets SERVER_TRUSTED_PROXIES, including an empty value, it replaces the yaml list. Check with: systemctl show sub2api -p Environment',
      dockerTitle: 'Docker Compose / docker-deploy.sh',
      dockerWhere: 'Set SERVER_TRUSTED_PROXIES in deploy/.env.',
      dockerStep1: 'Go to the deploy directory (usually deploy/ in the repository).',
      dockerStep2: 'Check for an existing value: grep \'^SERVER_TRUSTED_PROXIES=\' .env',
      dockerStep3: 'List only the reverse-proxy address or network that connects to the sub2api container, for example 172.18.0.0/16. Do not use a Cloudflare range.',
      dockerStep4: 'If the container port is published directly with no reverse proxy, set SERVER_TRUSTED_PROXIES=',
      dockerStep5: 'Optional recovery list: SERVER_IP_ACCESS_EMERGENCY_ALLOWLIST=YOUR.FIXED.PUBLIC.IP/32',
      dockerStep6: 'Recreate the container with compose up -d so it picks up the new env. A plain restart may not.',
      dockerRestart: 'docker compose -f docker-compose.local.yml up -d',
      dockerLogs: 'docker compose -f docker-compose.local.yml logs -f --tail=80 sub2api',
      dockerFileHint: 'Use the same compose file you originally started with (docker-compose.yml or docker-compose.local.yml).',
      standaloneTitle: 'Self-built binary, no systemd',
      standaloneWhere: 'Prefer a config.yaml in the process working directory (the binary looks in the current directory and ./config). Official install.sh uses /opt/sub2api/config.yaml.',
      standaloneStep1: 'Write the same server.trusted_proxies field used by the official binary.',
      standaloneStep2: 'Stop the old process and start a new one. Reloading the web page does not load this list.',
      proxyTitle: 'The reverse proxy must sanitize forwarding headers',
      proxyHint: 'The app accepts exactly one address from the direct trusted proxy. Do not use $proxy_add_x_forwarded_for. If you only rewrite headers and skip Real IP, the resolved client IP will be a Cloudflare egress address such as 162.158.* or 172.64–172.71.',
      proxyCloudflareHint: 'For Cloudflare orange-cloud, first confirm nginx -V includes --with-http_realip_module. Then enable Nginx Real IP in the http block and list official Cloudflare CIDRs with set_real_ip_from. A comment is not a configuration. Recheck https://www.cloudflare.com/ips-v4 and https://www.cloudflare.com/ips-v6 before going live.',
      proxyCloudflareHttp: 'underscores_in_headers on;\n\nreal_ip_header CF-Connecting-IP;\nreal_ip_recursive on;\n\nset_real_ip_from 173.245.48.0/20;\nset_real_ip_from 103.21.244.0/22;\nset_real_ip_from 103.22.200.0/22;\nset_real_ip_from 103.31.4.0/22;\nset_real_ip_from 141.101.64.0/18;\nset_real_ip_from 108.162.192.0/18;\nset_real_ip_from 190.93.240.0/20;\nset_real_ip_from 188.114.96.0/20;\nset_real_ip_from 197.234.240.0/22;\nset_real_ip_from 198.41.128.0/17;\nset_real_ip_from 162.158.0.0/15;\nset_real_ip_from 104.16.0.0/13;\nset_real_ip_from 104.24.0.0/14;\nset_real_ip_from 172.64.0.0/13;\nset_real_ip_from 131.0.72.0/22;\n\nset_real_ip_from 2400:cb00::/32;\nset_real_ip_from 2606:4700::/32;\nset_real_ip_from 2803:f800::/32;\nset_real_ip_from 2405:b500::/32;\nset_real_ip_from 2405:8100::/32;\nset_real_ip_from 2a06:98c0::/29;\nset_real_ip_from 2c0f:f248::/32;',
      proxyHeaderHint: 'After Real IP is applied, $remote_addr is the client public IP. Overwrite headers on every proxy_pass route; do not append:',
      proxyNginx: 'proxy_set_header X-Real-IP $remote_addr;\nproxy_set_header X-Forwarded-For $remote_addr;\nproxy_set_header CF-Connecting-IP "";',
      proxyVerify: 'Run nginx -t and reload. Confirm the http block has real directives with nginx -T | grep -E \'real_ip_header|set_real_ip_from\'. Back on this page, the resolved client IP must be your public address, not 162.158.* or 172.64–172.71.',
      proxyTunnelHint: 'For Cloudflare Tunnel, public Cloudflare CIDRs do not connect to Nginx. set_real_ip_from must list the actual cloudflared peer, commonly 127.0.0.1 and ::1 on the same host. Do not copy only the public Cloudflare ranges.',
      afterTitle: 'Restart, verify this page, then enable the switches',
      afterHint: 'Return here and check Trusted Proxy Status: the badge is Configured or Explicitly Disabled, the resolved client IP is your public address (not a Cloudflare egress IP), and the enforcement identity is safe. Only then turn on the system-settings master switch, and finally enable enforcement on this page.',
      warningsTitle: 'Do not do this',
      warningCloudflare: 'Do not put Cloudflare CIDRs, 0.0.0.0/0, ::/0, or * in the application trusted-proxy list. Cloudflare does not connect to this process; official CIDRs belong only in Nginx set_real_ip_from.',
      warningEnv: 'Do not set both the yaml file and SERVER_TRUSTED_PROXIES. If the environment variable exists, even empty, it replaces the file.',
      warningLegacy: 'The legacy forwarded-header compatibility switch does not control global IP enforcement. The global policy uses only server.trusted_proxies.',
      warningWizard: 'Do not rerun the setup wizard to rewrite the config file. It will drop trusted_proxies.',
    }
  }
}
